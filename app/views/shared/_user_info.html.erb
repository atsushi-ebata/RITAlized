<a href="<%= user_path(current_user) %>">
  <%= gravatar_for current_user, size: 52 %>
</a>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to  t('view.user_info.viewmyprofile'), current_user %>
</span>
<span>
  <%= t('view.user_profile.microposts') %> (<%= current_user.microposts.count %>)
</span>
